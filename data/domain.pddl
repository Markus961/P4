(define (domain grid)

(:requirements :strips)

(:predicates
    ; Predicates used for grid_extension are inferred, meaning that they appear without the user typing them here
    )

; Actions used for user-friendliness in grid_extension are inferred, meaning that they appear without the user typing them here

)